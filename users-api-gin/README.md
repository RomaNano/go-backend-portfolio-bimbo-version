# Users API (Gin)

REST API service for managing users and their profiles.

## Features
- Gin HTTP framework
- Layered architecture (handler / service / repository)
- PostgreSQL
- Configuration via environment variables
- Middleware support

## Run locally

```bash
docker-compose up --build
